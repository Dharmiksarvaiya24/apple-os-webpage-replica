# Pages

Drop page-level components into this folder. Keeping a dedicated `pages/` directory makes it easier to map new routes (home, product, iPhone, fallbacks) to the hero/feature/animation sections that live elsewhere.
