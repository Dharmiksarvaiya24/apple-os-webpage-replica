export default function HeroSectionPlaceholder() {
  return (
    <section className="section-hero" aria-label="Hero section placeholder">
      Hero section placeholder (empty layout ready for high-level copy).
    </section>
  )
}
