export default function FeatureSectionPlaceholder() {
  return (
    <section className="section-features" aria-label="Feature section placeholder">
      Feature section placeholder (ready for cards, lists, or spotlight content).
    </section>
  )
}
