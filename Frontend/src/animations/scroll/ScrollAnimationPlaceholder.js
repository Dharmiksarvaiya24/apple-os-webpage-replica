// Stub to represent where scroll animation helpers will live.
export function initScrollAnimations() {
  return {
    status: 'idle',
    description: 'hook up GSAP, ScrollTrigger, or IntersectionObserver helpers here.'
  }
}
